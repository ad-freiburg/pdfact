package icecite.models;

/**
 * An interface that declares that the implementing object has multiple
 * elements.
 *
 * @param <T>
 *        The concrete type of the PDF elements.
 *
 * @author Claudius Korzen
 */
public interface HasPdfElements<T extends PdfElement> {
  // /**
  // * Returns the most common height over all PDF elements.
  // *
  // * @return The most common height over all PDF elements.
  // */
  // float getMostCommonHeight();
  //
  // /**
  // * Returns the average height over all PDF elements.
  // *
  // * @return The average height over all PDF elements.
  // */
  // float getAverageHeight();
  //
  // //
  // ========================================================================
  //
  // /**
  // * Returns the most common width over all PDF elements.
  // *
  // * @return The most common width over all PDF elements.
  // */
  // float getMostCommonWidth();
  //
  // /**
  // * Returns the average width over all PDF elements.
  // *
  // * @return The average width over all PDF elements.
  // */
  // float getAverageWidth();
}